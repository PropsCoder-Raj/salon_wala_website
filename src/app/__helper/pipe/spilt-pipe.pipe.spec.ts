import { SpiltPipePipe } from './spilt-pipe.pipe';

describe('SpiltPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SpiltPipePipe();
    expect(pipe).toBeTruthy();
  });
});
